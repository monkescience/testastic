name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  GOLANGCI_LINT_VERSION: v2.11.3 # renovate: datasource=github-releases depName=golangci/golangci-lint

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6
        with:
          go-version-file: go.mod
      - uses: golangci/golangci-lint-action@1e7e51e771db61008b38414a730f564565cf7c20 # v9
        with:
          version: ${{ env.GOLANGCI_LINT_VERSION }}
          args: --timeout=5m

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6
        with:
          go-version-file: go.mod

      - name: Run tests with coverage
        run: go test -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Publish coverage summary
        shell: bash
        run: |
          set -euo pipefail

          COVERAGE=$(go tool cover -func=coverage.out | awk '/^total:/ {gsub("%", "", $3); print $3}')
          {
            echo "## Test Coverage"
            echo ""
            echo "| Metric | Value |"
            echo "|---|---:|"
            echo "| Current run | ${COVERAGE}% |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Check coverage delta
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail

          coverage_from_file() {
            go tool cover -func="$1" | awk '/^total:/ {gsub("%", "", $3); print $3}'
          }

          PR_COVERAGE=$(coverage_from_file coverage.out)

          BASE_DIR="$(mktemp -d)"
          git fetch --no-tags --depth=1 origin "${{ github.base_ref }}"
          git worktree add "$BASE_DIR" "origin/${{ github.base_ref }}"
          trap 'git worktree remove --force "$BASE_DIR"' EXIT

          pushd "$BASE_DIR" >/dev/null
          if go test -race -coverprofile=base_coverage.out -covermode=atomic ./...; then
            BASE_COVERAGE=$(coverage_from_file base_coverage.out)
          else
            echo "::warning::Base branch tests failed; using PR coverage as baseline"
            BASE_COVERAGE="$PR_COVERAGE"
          fi
          popd >/dev/null

          echo "Base branch coverage: ${BASE_COVERAGE}%"
          echo "PR coverage: ${PR_COVERAGE}%"
          DELTA=$(awk "BEGIN { printf \"%+.2f\", $PR_COVERAGE - $BASE_COVERAGE }")

          RESULT="PASS"

          if awk "BEGIN { exit !($PR_COVERAGE < $BASE_COVERAGE) }"; then
            RESULT="FAIL"
            echo "::error title=Coverage gate failed::Coverage decreased from ${BASE_COVERAGE}% to ${PR_COVERAGE}% (delta: ${DELTA}%)"
          else
            echo "::notice title=Coverage gate passed::Coverage is ${PR_COVERAGE}% (base: ${BASE_COVERAGE}%, delta: ${DELTA}%)"
          fi

          {
            echo ""
            echo "## Coverage Gate"
            echo ""
            echo "| Metric | Value |"
            echo "|---|---:|"
            echo "| Base (${{ github.base_ref }}) | ${BASE_COVERAGE}% |"
            echo "| PR | ${PR_COVERAGE}% |"
            echo "| Delta | ${DELTA}% |"
            echo "| Rule | PR >= Base |"
            echo "| Result | ${RESULT} |"
          } >> "$GITHUB_STEP_SUMMARY"

          if [ "$RESULT" = "FAIL" ]; then
            exit 1
          fi
